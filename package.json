{
  "name": "schedule-parser-v2",
  "version": "2.0.0",
  "description": "Wedding Photography Schedule Management System (Monorepo)",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "lint": "npm run lint --workspace=frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
