# Backend API URL
VITE_API_URL=http://localhost:8000

# Note: OAuth Client IDs are now managed by the backend
# The frontend fetches them from /api/config endpoint at runtime
# No need to set VITE_GOOGLE_CLIENT_ID, VITE_NAVER_CLIENT_ID, or VITE_KAKAO_REST_API_KEY here
